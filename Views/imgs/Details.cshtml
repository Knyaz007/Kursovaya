@model Kursovaya.Models.img

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Детали</h1>

<div>
    <h4>img</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @if (ViewBag.Photodata != null)
            {
                <img class="mt-4 mb-4" width="100%" src="data:image/jpeg;base64,@(Convert.ToBase64String(ViewBag.Photodata))">
            }
          @*  @Html.DisplayNameFor(model => model.Photo)*@
        </dt>
        <dd class = "col-sm-10">
           @* @Html.DisplayFor(model => model.Photo)

            <img src="@Html.DisplayFor(model => model.Photo)" alt="Изображение" />*@
        </dd>
        @*<img src="@Url.Content(Model.Photo)" alt="нету" />
        <input type="hidden" name="Photo" value="@Model.Photo" />*@
       

    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Img_id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
